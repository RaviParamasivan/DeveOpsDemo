package com.hackathon.jira;

import java.net.URI;
import java.net.URISyntaxException;


public class Jira {

	public boolean updateJira() throws URISyntaxException {	
/*		Issue issue =null;
		try{
	        JiraRestClientFactory factory = new JiraRestClientFactory();
	        URI jiraServerUri = new URI("https://jigarthandaone.atlassian.net/rest/api/2/search?jql");
	        JiraRestClient restClient = factory.createWithBasicHttpAuthentication(jiraServerUri, "senhan84@gmail.com", "bluemount");
	        NullProgressMonitor pm = new NullProgressMonitor();
	        issue = restClient.getIssueClient().getIssue("TST-1", pm);
		}catch(Exception e){
			e.printStackTrace();
		}
        
        System.out.println(issue);
        */
		return false;
	}

}
